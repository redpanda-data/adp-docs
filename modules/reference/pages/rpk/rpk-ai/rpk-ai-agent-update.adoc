= rpk ai agent update
:description: rpk ai agent update
:page-beta: true

Update an agent

== Usage

[,bash]
----
rpk ai agent update <name> [flags]
----

== Flags

[cols="1m,1a,2a"]
|===
|*Value* |*Type* |*Description*

|--description |string |new description.

|--display-name |string |new display name.

|-h, --help |- |help for update.

|--llm-provider |string |name of the LLM provider resource the agent uses.

|--max-iterations |int32 |maximum agent loop iterations per invocation.

|--mcp-server |strings |MCP server resource name the agent may call (repeatable).

|--model |string |model the agent runs (e.g. claude-sonnet-4-6).

|--system-prompt |string |system prompt for the agent.

|--tag |stringToString |replacement metadata tags key=value (repeatable) (default []).

|-o, --format |string |output format: table{vbar}wide{vbar}json{vbar}yaml{vbar}markdown (env: RPAI_FORMAT) (default "table").

|--no-color |- |disable colored output (env: NO_COLOR).

|-c, --rpai-config |string |path to rpai config (env: RPAI_CONFIG) (default "/var/lib/redpanda/.rpai/config").

|-s, --rpai-endpoint |string |override the selected environment's AI Gateway URL for this invocation.

|-p, --rpai-profile |string |rpai profile name (env: RPAI_PROFILE).

|-v, --rpai-verbose |- |verbose debug logging to stderr (env: RPAI_VERBOSE).

|--token |string |static bearer token override (ambient RPAI_TOKEN is ignored under rpk ai).
|===